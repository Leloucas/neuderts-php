angular.module('neuderts').controller('newBlogCtrl', newBlogCtrl);

newBlogCtrl.$inject = ['$location', 'neuData', '$routeParams'];

function newBlogCtrl($location, neuData, $routeParams){
  var vm = this;
}
